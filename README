Синтаксис запроса к серверу, длины комманды и ключа 3 и 4 байта соответственно.
Между ними разделить двоеточие ":" 1 байт, общая длина 8 байт:
command:key 

GET:1234
    возвращает случайный неиспользуемый ключ, поле для ключа
    нужно забить чем угодно длиной в 4 байта

REP:1234
    гасит(repaid) переданный ключ если тот используется, в
    противном случае возвращает "key not repayed"

CHE:1234
    вывод информацию о переданном ключе, а именно
    not issued - не используется
    issued - используется
    repaid - погашен

INF:1234
    возвращает количество оставшихся свободных(not issued) ключей,
    поле для ключа нужно забить чем угодно длиной в 4 байта


Вначале нужно запустить create_collection_keys.py что бы создалась коллекция
"all_keys" содержащая все ключ.
